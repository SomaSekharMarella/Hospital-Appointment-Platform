import React from 'react';
import { Container, Typography } from '@mui/material';

const SuccessPage: React.FC = () => {
  return (
    <Container maxWidth="sm">
      <Typography variant="h4">Booking Successful!</Typography>
    </Container>
  );
};

export default SuccessPage; 